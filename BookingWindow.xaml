<Window x:Class="beauty_salon.BookingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:beauty_salon"
        mc:Ignorable="d"
        Title="Запись клиента на услугу" Height="400" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Услуга:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBlock x:Name="txtService" Grid.Row="1" Margin="0,0,0,10" FontWeight="Bold"/>

        <TextBlock Text="Клиент:" Grid.Row="2" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbClient" Grid.Row="3" DisplayMemberPath="FullName" Margin="0,0,0,10"/>

        <TextBlock Text="Дата и время:" Grid.Row="4" Margin="0,0,0,5"/>
        <DatePicker x:Name="dpDate" Grid.Row="5" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbTime" Grid.Row="6" Margin="0,0,0,10">
            <ComboBoxItem Content="09:00"/>
            <ComboBoxItem Content="10:00"/>
            <ComboBoxItem Content="11:00"/>
            <ComboBoxItem Content="12:00"/>
            <ComboBoxItem Content="13:00"/>
            <ComboBoxItem Content="14:00"/>
            <ComboBoxItem Content="15:00"/>
            <ComboBoxItem Content="16:00"/>
            <ComboBoxItem Content="17:00"/>
            <ComboBoxItem Content="18:00"/>
        </ComboBox>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Click="BtnSave_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="BtnCancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
