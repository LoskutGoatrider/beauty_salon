<Window x:Class="beauty_salon.EditServiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:beauty_salon"
        mc:Ignorable="d"
        Title="Редактирование услуги" 
        Height="550" 
        Width="450"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize"
        Background="{StaticResource PrimaryBackgroundBrush}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="ID:" Grid.Row="0" Grid.Column="0" 
                   VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox x:Name="txtId" Grid.Row="0" Grid.Column="1" 
                 IsEnabled="False" Margin="0,0,0,10"/>

        <TextBlock Text="Название:" Grid.Row="1" Grid.Column="0" 
                   VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox x:Name="txtTitle" Grid.Row="1" Grid.Column="1" 
                 Margin="0,0,0,10"/>

        <TextBlock Text="Стоимость:" Grid.Row="2" Grid.Column="0" 
                   VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox x:Name="txtCost" Grid.Row="2" Grid.Column="1" 
                 Margin="0,0,0,10"/>

        <TextBlock Text="Длительность (минуты):" Grid.Row="3" Grid.Column="0" 
                   VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox x:Name="txtDuration" Grid.Row="3" Grid.Column="1" 
                 Margin="0,0,0,10"/>

        <TextBlock Text="Описание:" Grid.Row="4" Grid.Column="0" 
                   VerticalAlignment="Top" Margin="0,0,10,5"/>
        <TextBox x:Name="txtDescription" Grid.Row="4" Grid.Column="1" 
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" 
                 MinHeight="60" Margin="0,0,0,10"/>

        <TextBlock Text="Скидка (0.0 - 1.0):" Grid.Row="5" Grid.Column="0" 
                   VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox x:Name="txtDiscount" Grid.Row="5" Grid.Column="1" 
                 Margin="0,0,0,10"/>

        <TextBlock Text="Изображение:" Grid.Row="6" Grid.Column="0" 
                   VerticalAlignment="Center" Margin="0,0,10,5"/>
        <StackPanel Grid.Row="6" Grid.Column="1" 
                    Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnSelectImage" Content="Выбрать" 
                    Click="BtnSelectImage_Click" 
                    Margin="0,0,10,0"
                    Background="{StaticResource SecondaryBackgroundBrush}"/>
            <TextBlock x:Name="txtImagePath" VerticalAlignment="Center"/>
        </StackPanel>

        <Image x:Name="imgPreview" Grid.Row="7" Grid.Column="1" 
               Height="100" Margin="0,0,0,15" 
               HorizontalAlignment="Left" Stretch="Uniform"/>

        <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <Button Content="Сохранить" Click="BtnSave_Click" 
                    Width="100" Margin="0,0,10,0"
                    Background="{StaticResource AccentBrush}" 
                    Foreground="White"/>
            <Button Content="Отмена" Click="BtnCancel_Click" 
                    Width="100"
                    Background="{StaticResource SecondaryBackgroundBrush}"/>
        </StackPanel>
    </Grid>
</Window>